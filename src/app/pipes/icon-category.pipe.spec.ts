import { IconCategoryPipe } from './icon-category.pipe';

describe('IconCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new IconCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
