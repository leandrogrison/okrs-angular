import { ColorProgressPipe } from './color-progress.pipe';

describe('ColorProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
